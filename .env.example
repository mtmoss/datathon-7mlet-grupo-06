# Chave da API da Anthropic (Claude) usada pelo assistente RAG.
# Pegue em https://console.anthropic.com  (NUNCA versione a chave real)
ANTHROPIC_API_KEY=
# Modelo Claude usado pelo assistente (ajuste conforme sua conta)
ANTHROPIC_MODEL=claude-3-5-sonnet-latest

# Tracking local do MLflow
MLFLOW_TRACKING_URI=sqlite:///mlflow.db
